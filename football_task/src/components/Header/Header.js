import React from 'react';
import { Col, Container,  Row } from 'reactstrap';

const Header = (props) => {
    return (
        <header>
            <Container>
            <p>
                LOGO
            </p>
            </Container>
        </header>
           
    );
}

export default Header;