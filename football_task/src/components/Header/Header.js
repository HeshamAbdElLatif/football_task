import React from 'react';
import './Header.scss';
import { Col, Container,  Row } from 'reactstrap';

const Header = (props) => {
    return (
        <header>
            <Container>
            <p>
                LOGO
            </p>
            </Container>
        </header>
           
    );
}

export default Header;